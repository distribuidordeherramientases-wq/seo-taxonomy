name: Temporal lint campaign autodetect

on:
  push:
    branches:
      - fix/2.3.8.1-campaign-product-autodetect

jobs:
  php-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: PHP syntax
        run: php -l includes/marketing/campaigns.php
